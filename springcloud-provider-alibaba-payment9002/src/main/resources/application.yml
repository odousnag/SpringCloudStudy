server:
  port: 9002

spring:
  application:
    name: nacos-provider
  cloud:
    nacos:
      discovery:
        # 配置 Nacos 地址
        server-addr: localhost:8848

# 暴露服务
management:
  endpoints:
    web:
      exposure:
        include: '*'
